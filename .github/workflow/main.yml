name: CI
on: pull_request
jobs:
  test:
    name: Check the source code
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v2
      - name: Install packages
        run: npm ci
      - name: Prettier
        run: npm run format
      - name: Lint
        run: npm run lint